import React, { Component } from "react";
import PropTypes from 'prop-types';

/*
class Form extends Component {
	constructor(props, context) {
		super(props);
		this.state = {
			
		}
	}
	render() {
		return (
			<form onSubmit={this.props.bindsubmit}>
				{this.props.children}
			</form>
		)
	}
}

class Input extends Component {
	constructor(props, context) {
		super(props);
		this.state = {
			value: props.value
		}
	}
	handleChange(e) {
		this.setState({
			value: e.target.value
		})
		console.dir(e.target.value);
	}
	render() {
		return (
			<input type={this.props.type} placeholder={this.props.placeholder} value={this.state.value} onChange={this.handleChange.bind(this)}></input>
		)
	}
}

class Button extends Component {
	constructor(props, context) {
		super(props);
		this.state = {
			
		}
	}
	render() {
		return (
			<button type={this.props.type}>{this.props.children}</button>
		)
	}
}

Input.propTypes = {
	text: PropTypes.string
}
export {
	Form, Input, Button
};
*/